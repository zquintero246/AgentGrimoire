{
  "name": "calculate",
  "description": "Evaluate an arithmetic expression safely (no code execution).",
  "entrypoint": "spell.py:calculate",
  "parameters": {
    "type": "object",
    "properties": {
      "expression": {
        "type": "string",
        "description": "Arithmetic expression, e.g. '(2 + 3) * sqrt(16)'."
      }
    },
    "required": ["expression"]
  }
}
